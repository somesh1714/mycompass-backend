spring.application.name=mycompass-backend

spring.datasource.url=jdbc:postgresql://localhost:5432/mycompass
spring.datasource.username=mycompass
spring.datasource.password=mycompass

# Local dev only - move to an environment variable before any real deployment
app.jwt.secret=dev-only-secret-key-change-before-any-real-deployment-32bytes-min
app.jwt.expiration-ms=86400000

# Replace with a real OAuth Client ID from Google Cloud Console - see docs/frontend
# implementation plan for setup steps. Google sign-in will fail verification until this is real.
# NOT WORKING YET: we have not obtained a Client ID from Google - every Google sign-in
# attempt will fail with "Invalid Google token" until this placeholder is replaced with
# the real value. Google sign-in will start working immediately once it is.
app.google.client-id=REPLACE_WITH_GOOGLE_OAUTH_CLIENT_ID
