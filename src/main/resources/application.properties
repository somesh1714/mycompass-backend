spring.application.name=mycompass-backend

spring.datasource.url=jdbc:postgresql://localhost:5432/mycompass
spring.datasource.username=mycompass
spring.datasource.password=mycompass

# Local dev only — move to an environment variable before any real deployment
app.jwt.secret=dev-only-secret-key-change-before-any-real-deployment-32bytes-min
app.jwt.expiration-ms=86400000
